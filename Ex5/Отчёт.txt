а) 1 - когда новый объект инициализируется существующим объектом явно, 2 - когда программа генерирует копию объекта.
b) Дочерний класс наследует как поведение (методы), так и свойства (переменные-члены) от родителя.
c) Предоставляет механизм для освобождения неуправляемых ресурсов.
d) Да.
e) Через двоеточие прописать параметры, наследуемые от родительского класса.
f) Используя ключевое слово "base".
g) Для этого при создании члена класса-агрегата нужно указать необходимые параметры.
h) C++:
	Ссылка не может ссылаться на объект;
	При передаче параметра по ссылке передаётся адрес объекта, на который сослались при вызове метода;
	О наличии ссылок на объект в программе узнать нельзя;
   C#:
	Ссылка является самостоятельной сущностью;
	При передаче в качестве параметра ссылка копируется;
	За ссылкамии следит сборщик мусора;
i) Перед блоком кода с классом надо прописать "template <typename T>" 